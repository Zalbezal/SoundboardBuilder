function play_sound0 (clicked_id) {
        var audio = new Audio(clicked_id + ".mp3"); 
        audio.play();
      }
      function play_sound1 (clicked_id) {
        var audio = new Audio(clicked_id + ".wav"); 
        audio.play();
      }
      